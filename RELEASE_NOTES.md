# CodexQuotaTaskbar v0.1.5

发布日期：2026-07-31

这是修复 Windows 登录开机启动竞态的维护版本。它保持任务栏宿主、额度显示、设置格式和只读网络行为不变。

## 本次更新

- 修复启用“开机启动”后，Windows 登录阶段因 Explorer 的任务栏、通知区或任务按钮结构尚未就绪而立即弹错退出的问题。
- 首次附着增加结构化失败分类；只对可能暂时未就绪的失败进行最多 6 次静默重试，第一次等待 1.5 秒，之后每次等待 3 秒。
- 等待期间继续处理 Windows 消息；每次完整重探测前停止监听并清理可能部分附着的子窗口。
- 明确不支持的任务栏布局、系统安全空白不足或外部障碍耗尽空间仍立即提示原因，不会被重试掩盖。
- 新增启动附着策略自动化，覆盖暂时失败后恢复、永久失败立即返回、分段等待和次数上限。

## 验证状态

本机 Windows 11 build `22631.6199`、96 DPI、单显示器环境已完成：

- Visual Studio 2026 x64 Release 全量构建和 CTest 7/7；启动重试策略及原有生产逻辑、布局、碰撞、渲染和解析测试全部通过。
- `TaskbarProbe` 返回 `supported`，稳定性探针 19/19、最终交互探针 21/21，生命周期探针分别在 47 毫秒和 16 毫秒内完成清理退出。
- 受控重启 Explorer 后，在新任务栏结构仍未就绪时启动 v0.1.5：程序在通知区与 UI Automation 边界仍为 0×0 时保持运行，结构就绪后自动附着为 84×42 的主任务栏子窗口，随后 `TaskbarProbe` 转为 `supported`。这直接覆盖了本次修复的任务栏就绪竞态，但不等同于完整注销/登录。
- Windows 2022 / Visual Studio 2022 的构建、测试与打包以正式标签对应的 GitHub Actions 为最终门槛。

本次发布没有在当前会话中执行真实注销再登录。产品负责人在知悉这一验证缺口后要求立即发布，因此该项作为已接受但未实测的风险保留，不得理解为已经通过。

本机未安装 TrafficMonitor，本版本未重新执行该兼容性专项；相关兼容性只保留 v0.1.2 的历史验证结果，不扩大当前版本承诺。v0.1.4 已完成的单额度标签、全屏、截图、任务栏自动隐藏和 Explorer 重启验证只作为历史回归基线，不冒充 v0.1.5 结果。

## 已知限制

- 真实 Windows 注销/登录的开机启动路径尚未用 v0.1.5 完成一次端到端复核；已完成“Explorer 停止后先启动程序、再等待任务栏恢复”的受控竞态验证。若仍遇到弹框，请附上不含账号和 Token 的错误文字与 Windows 版本。
- v0.1.5 未重新运行 TrafficMonitor、全屏、截图、任务栏自动隐藏、Explorer 重启、125% / 150% / 200% DPI、双显示器和睡眠恢复专项；这些项目只保留历史结果，不扩大支持承诺。
- 具体游戏、干净 Windows 用户环境及其他 Windows 11 内部版本仍未逐项验证。
- 当前 EXE 未进行代码签名，Windows 可能显示“未知发布者”或 SmartScreen 提示。
- 使用的是 OpenAI 未公开的 `chatgpt.com/backend-api/wham/` 后端路径，没有长期兼容性承诺。
- 没有安装器、自动更新、Windows 10 支持、多账号、Token 自动刷新或多任务栏显示。

## 安装

可直接下载 `CodexQuotaTaskbar-v0.1.5-win-x64.exe`，也可以下载 `CodexQuotaTaskbar-v0.1.5-win-x64.zip` 后解压运行，并使用 `SHA256SUMS.txt` 核对两者。目标电脑需安装 Microsoft Visual C++ 2015–2026 x64 运行库。

完整发布包只允许包含正式 EXE、README、发行说明、MIT License、第三方声明和 nlohmann/json MIT License，不得包含凭证、账号数据、响应转储、测试固件、参考仓库或构建缓存。
