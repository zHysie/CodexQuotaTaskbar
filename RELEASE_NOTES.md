# CodexQuotaTaskbar v0.1.0

发布日期：2026-07-17

这是 CodexQuotaTaskbar 的第一个正式版。它在 Windows 11 主任务栏中显示当前 Codex 账号的 5 小时与周剩余额度，并提供原生 Tooltip、右键设置、后台刷新、单实例和当前用户开机启动。

## 主要功能

- 只读使用当前 Windows 用户已有的 Codex 登录状态，不写回 Token。
- 只请求 `chatgpt.com` 下两个固定额度路径，拒绝重定向并限制响应大小。
- 支持上下两行与单行显示、分别显示 5 小时或周额度、1 / 3 / 5 / 10 / 30 分钟刷新间隔和多种颜色模式。
- 断网、限流或服务异常时保留最后成功额度，并按有界退避恢复。
- Explorer 重建后有界重新附着；无法安全找到任务栏空白区时提示并退出，不降级为悬浮窗。
- 与 TrafficMonitor 一类任务栏悬浮窗口发生占用冲突时自动避让。

## v0.1.0 修复

- 单行只显示一个额度时，字号与固定三列布局现在和上下两行模式一致。
- 单行与上下两行之间切换改为原窗口立即重绘，不再短暂隐藏文字。
- 修复 DirectComposition 失败恢复、凭证目录创建/删除/重建监听、旧开机启动路径误判、高 DPI 字号缩放和窄宽度单行裁切。
- 完善失败状态优先级、未知重置时间、刷新去重、退出竞态和任务栏变化监听。

## 已验证基线

- Windows 11 x64 build `10.0.26200.8737`
- 100% DPI，单显示器 2560×1440
- 底部、非自动隐藏主任务栏
- Visual Studio 2022 x64 Release 与 CTest 6/6
- 任务栏交互探针 21/21、单实例、正常清理退出
- Explorer 人工重启后恢复显示，浏览器和任务栏正常，实例数保持一个

## 已知限制

- v0.1.0 的支持承诺限定为 Windows 11 x64、100% DPI、单显示器、底部非自动隐藏主任务栏。
- 125% / 150% / 200% DPI、双显示器与主显示器切换、自动隐藏、睡眠恢复、干净 Windows 用户环境及其他 Windows 11 内部版本尚未完成真实验证。
- 当前 EXE 未进行代码签名，Windows 可能显示“未知发布者”或 SmartScreen 提示。
- 使用的是 OpenAI 未公开的 `chatgpt.com/backend-api/wham/` 后端路径，没有长期兼容性承诺；接口变化时软件可能暂时无法刷新。
- 没有安装器、自动更新、Windows 10 支持、多账号、Token 自动刷新或多任务栏显示。

## 安装

下载 `CodexQuotaTaskbar-v0.1.0-win-x64.zip`，核对 Release 页面中的 SHA-256，解压后运行 `CodexQuotaTaskbar.exe`。目标电脑需安装 Microsoft Visual C++ 2015–2022 x64 运行库。

发布包包含正式 EXE、README、发行说明、MIT License、第三方声明和 nlohmann/json MIT License，不包含凭证、账号数据、响应转储、测试固件、参考仓库或构建缓存。
