# CodexQuotaTaskbar v0.1.3

发布日期：2026-07-27

这是针对麦克风等系统状态按钮消失后额度窗口仍停留在左侧的维护版本。它继续保持 v0.1.2 的任务栏稳定策略，以及只读访问 Codex 额度、不写回 Token、不消费重置机会的安全边界。

## 本次修复

- 麦克风等系统状态按钮使 `TrayNotifyWnd` 临时变宽时，继续沿用稳定碰撞确认并使用同一 HWND 向左安全避让。
- 状态按钮消失、通知区明确收缩后，临时以 250 毫秒间隔执行轻量复核；最右安全目标连续稳定 1 秒后自动复位，随后恢复 2 秒常规检查。
- 自动复位不调用完整 UI Automation 探测，也不执行硬重附着；移动后若安全校验失败会回滚原位置。
- TrafficMonitor 等外部顶层工具消失仍不会触发向右回抢，避免恢复能力重新引入位置来回跳动。
- 新增稳定恢复状态测试，覆盖稳定等待、目标抖动、机会消失和目标变化后重新计时。

## 验证结果

- 本机独立构建目录完成 CTest 6/6，`TaskbarCollisionTests` 32/32 通过。
- 用户在真实 Windows 11 桌面确认：麦克风状态按钮出现时额度窗口安全避让，按钮消失后约 1 秒自动复位，期间保持同一 HWND。
- v0.1.2 已完成的全屏、截图、TrafficMonitor、任务栏自动隐藏、Explorer 重启、交互与生命周期验证继续作为回归基线；v0.1.3 未重新执行的环境组合不扩大支持承诺。

## 已知限制

- 本机 `ms-screenclip` 关联损坏，Win+Shift+S 会弹出“打开方式”；v0.1.2 已改用 SnippingTool Ctrl+N 验证真实截图遮罩行为，v0.1.3 未重新执行该专项。
- v0.1.3 未重新验证具体游戏、125% / 150% / 200% DPI、双显示器和睡眠恢复，只保留既有历史结果。
- 干净 Windows 用户环境及其他 Windows 11 内部版本尚未完成真实验证。
- 当前 EXE 未进行代码签名，Windows 可能显示“未知发布者”或 SmartScreen 提示。
- 使用的是 OpenAI 未公开的 `chatgpt.com/backend-api/wham/` 后端路径，没有长期兼容性承诺；接口变化时软件可能暂时无法刷新。
- 没有安装器、自动更新、Windows 10 支持、多账号、Token 自动刷新或多任务栏显示。

## 安装

正式发布后可直接下载 `CodexQuotaTaskbar-v0.1.3-win-x64.exe`，也可以下载 `CodexQuotaTaskbar-v0.1.3-win-x64.zip` 后解压运行，并使用 `SHA256SUMS.txt` 核对两者。目标电脑需安装 Microsoft Visual C++ 2015–2026 x64 运行库。

完整发布包包含正式 EXE、README、发行说明、MIT License、第三方声明和 nlohmann/json MIT License，不包含凭证、账号数据、响应转储、测试固件、参考仓库或构建缓存。
